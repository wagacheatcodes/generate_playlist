name: Playlist Update (Fast Mode)

on:
  # Run every 6 hours
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write

    steps:
    - name: Checkout Public Data
      uses: actions/checkout@v3
      with:
        path: dist

    - name: Checkout Private Scripts
      uses: actions/checkout@v3
      with:
        repository: wagacheatcodes/Alpha
        token: ${{ secrets.API_TOKEN_GITHUB }}
        path: scripts_repo

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install & Setup
      run: |
        cp -r scripts_repo/scripts .
        cp scripts_repo/package.json .
        npm install

    - name: Run Generators
      env:
        TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
      run: |
        echo "--- 1. Updating Series List ---"
        node scripts/generate_series_from_tvs.js
        
        echo "--- 2. Updating Movie List ---"
        node scripts/generate_streams_from_movies.js
        
        echo "--- 3. Fetching Metadata ---"
        node scripts/enrich_metadata.js
        
        echo "--- 4. Scanning Episodes ---"
        node scripts/generate_episodes.js

    - name: Commit and Push
      working-directory: ./dist
      run: |
        git config user.name "Playlist Bot"
        git config user.email "bot@example.com"
        git add .
        if [[ -n $(git status -s) ]]; then
          git commit -m "Progress Update: $(date)"
          git push
        fi

    # CHECK FOR CONTINUATION FLAG
    - name: Check for Continuation
      id: check_continue
      run: |
        if [ -f "CONTINUE_SCAN_NEEDED" ]; then
          echo "continue=true" >> $GITHUB_OUTPUT
        else
          echo "continue=false" >> $GITHUB_OUTPUT
        fi

    # FIXED: Added --repo flag so it finds the repository correctly
    - name: Trigger Next Run
      if: steps.check_continue.outputs.continue == 'true'
      run: |
        echo "🚀 Time limit reached. Triggering immediate follow-up run..."
        gh workflow run weekly_update.yml --repo ${{ github.repository }}
      env:
        GH_TOKEN: ${{ secrets.API_TOKEN_GITHUB }}
